# M3.1 Dashboard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar dashboard nutricional do dia na Home (1 hero ring kcal + 3 macros), com Realtime de `daily_summaries` e `meals` via Supabase channels.

**Architecture:** Backend cria view `vw_today_summary` (security_invoker), função RPC `fitbrother_today()`, endpoint `GET /me/daily-summary`, e habilita publication Realtime nas duas tabelas. Mobile adiciona `ProgressRing` (react-native-svg + Reanimated 4), 3 hooks (`useDailySummary` + 2 Realtime), `TodaySummaryHeader`, e integra como `ListHeaderComponent` na Home.

**Tech Stack:** React Native 0.81 / Expo SDK 54 / Reanimated 4 / react-native-svg / NativeWind v4 / Fastify 5 / Supabase (Postgres + Realtime) / Zod / @fitbrother/shared.

**Spec base:** [docs/superpowers/specs/2026-05-24-m3-1-dashboard-design.md](../specs/2026-05-24-m3-1-dashboard-design.md)

**Branch:** `m3-1-dashboard` (já criada com spec commitada).

---

## File Structure

### Created
- `supabase/migrations/0022_daily_summary_helpers.sql` — RPC `fitbrother_today` + view `vw_today_summary`
- `supabase/migrations/0023_realtime_publication.sql` — `ALTER PUBLICATION ... ADD TABLE`
- `packages/shared/src/schemas/daily-summary.ts` — Zod schema + types
- `apps/mobile/lib/hooks/useDailySummary.ts` — React Query hook
- `apps/mobile/lib/hooks/useDailySummaryRealtime.ts` — Supabase channel subscription
- `apps/mobile/lib/hooks/useMealsRealtime.ts` — Supabase channel subscription
- `apps/mobile/components/domain/ProgressRing.tsx` — SVG animado
- `apps/mobile/components/domain/TodaySummaryHeader.tsx` — hero + 3 rings
- `scripts/checks/m3-1-daily-summary.sh` — script bash com 7 checks SQL/curl
- `scripts/checks/m3-1-daily-summary.sql` — assertions SQL referenciadas pelo bash

### Modified
- `packages/shared/src/index.ts` — re-export do novo schema
- `apps/server/src/routes/me.ts` — adiciona handler `GET /me/daily-summary`
- `apps/mobile/lib/api/me.ts` — `fetchDailySummary(day)` (criar se não existe; senão estende)
- `apps/mobile/lib/colors.ts` — adiciona alias `calories: colors.primary`
- `apps/mobile/app/(app)/index.tsx` — integra `TodaySummaryHeader` como `ListHeaderComponent`

---

## Task 1: Migration `fitbrother_today` + `vw_today_summary`

**Files:**
- Create: `supabase/migrations/0022_daily_summary_helpers.sql`

- [ ] **Step 1: Criar o arquivo de migration**

Criar `supabase/migrations/0022_daily_summary_helpers.sql`:

```sql
-- M3.1. "Hoje" do user respeitando timezone + day_start_hour.
-- Wrapper sobre fitbrother_nutritional_day (0014) sem ts argument.
CREATE OR REPLACE FUNCTION public.fitbrother_today(p_user_id uuid)
RETURNS date
LANGUAGE sql STABLE
AS $$
  SELECT public.fitbrother_nutritional_day(p_user_id, now());
$$;

-- View: row de daily_summaries para o dia "hoje" do user autenticado.
-- security_invoker=true → RLS de daily_summaries aplica ao caller.
-- Retorna 0 rows se user não tem refeições hoje.
CREATE OR REPLACE VIEW public.vw_today_summary
WITH (security_invoker = true)
AS
  SELECT ds.*
  FROM public.daily_summaries ds
  WHERE ds.user_id = auth.uid()
    AND ds.day = public.fitbrother_today(auth.uid());

-- GRANT SELECT pro role anon/authenticated (views novas não herdam por padrão).
GRANT SELECT ON public.vw_today_summary TO anon, authenticated;
```

- [ ] **Step 2: Aplicar migration no Supabase local**

```bash
cd /home/pedrobritto/development/fitbrother
npx supabase db reset
```

Expected: aplica TODAS as migrations sem erro, termina com "Finished supabase db reset on branch main."

- [ ] **Step 3: Verificar função existe e respeita boundary**

```bash
docker exec supabase_db_fitbrother psql -U postgres -d postgres -c \
  "SELECT public.fitbrother_today('00000000-0000-0000-0000-000000000000');"
```

Expected: retorna NULL (user inexistente) sem erro.

- [ ] **Step 4: Verificar view existe e tem security_invoker**

```bash
docker exec supabase_db_fitbrother psql -U postgres -d postgres -c \
  "SELECT viewname, definition FROM pg_views WHERE viewname='vw_today_summary';"
```

Expected: 1 row, definition contém `auth.uid()` e referência a `daily_summaries`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/0022_daily_summary_helpers.sql
git commit -m "feat(supabase): fitbrother_today() RPC + vw_today_summary view (M3.1)"
```

---

## Task 2: Migration Realtime publication

**Files:**
- Create: `supabase/migrations/0023_realtime_publication.sql`

- [ ] **Step 1: Criar o arquivo de migration**

Criar `supabase/migrations/0023_realtime_publication.sql`:

```sql
-- M3.1. Habilita Realtime (Logical Replication via publication
-- "supabase_realtime") em daily_summaries + meals.
--
-- Filtro de eventos por user_id acontece no canal Postgres Changes do
-- cliente. RLS valida que apenas rows pertencentes ao user vão pro
-- subscriber correto (security_invoker em views, owner_read em tabelas).
ALTER PUBLICATION supabase_realtime ADD TABLE public.daily_summaries;
ALTER PUBLICATION supabase_realtime ADD TABLE public.meals;
```

- [ ] **Step 2: Aplicar migration**

```bash
cd /home/pedrobritto/development/fitbrother
npx supabase db reset
```

Expected: aplica sem erro.

- [ ] **Step 3: Verificar publication tem as 2 tabelas**

```bash
docker exec supabase_db_fitbrother psql -U postgres -d postgres -c \
  "SELECT schemaname, tablename FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename IN ('daily_summaries','meals') ORDER BY tablename;"
```

Expected: 2 rows — `public/daily_summaries` e `public/meals`.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0023_realtime_publication.sql
git commit -m "feat(supabase): enable Realtime on daily_summaries + meals (M3.1)"
```

---

## Task 3: Zod schema em `@fitbrother/shared`

**Files:**
- Create: `packages/shared/src/schemas/daily-summary.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: Criar o schema**

Criar `packages/shared/src/schemas/daily-summary.ts`:

```ts
import { z } from "zod";

export const DailySummarySchema = z.object({
  user_id: z.string().uuid(),
  day: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  kcal: z.number(),
  protein_g: z.number(),
  carbs_g: z.number(),
  fat_g: z.number(),
  goal_kcal: z.number().nullable(),
  goal_protein_g: z.number().nullable(),
  goal_carbs_g: z.number().nullable(),
  goal_fat_g: z.number().nullable(),
  goal_hit: z.boolean(),
  meals_count: z.number().int(),
  updated_at: z.string(),
});

export type DailySummary = z.infer<typeof DailySummarySchema>;

export const DailySummaryResponseSchema = z.object({
  summary: DailySummarySchema,
});

export type DailySummaryResponse = z.infer<typeof DailySummaryResponseSchema>;
```

- [ ] **Step 2: Re-exportar do index**

Editar `packages/shared/src/index.ts` adicionando ao final:

```ts
export * from "./schemas/daily-summary.js";
```

- [ ] **Step 3: Verificar tipos passam**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/shared
```

Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add packages/shared/src/schemas/daily-summary.ts packages/shared/src/index.ts
git commit -m "feat(shared): DailySummary zod schema + types"
```

---

## Task 4: Backend route `GET /me/daily-summary`

**Files:**
- Modify: `apps/server/src/routes/me.ts`

- [ ] **Step 1: Estender meRoutes com novo handler**

Substituir o conteúdo de `apps/server/src/routes/me.ts` por:

```ts
import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { DailySummarySchema, type DailySummary } from "@fitbrother/shared";
import { authRequired, supabaseForRequest } from "../lib/auth.js";

const dailySummaryQuerySchema = z.object({
  day: z
    .string()
    .regex(/^\d{4}-\d{2}-\d{2}$/, "day must be YYYY-MM-DD")
    .optional(),
});

export async function meRoutes(app: FastifyInstance) {
  app.get("/me", { preHandler: [authRequired] }, async (req, reply) => {
    const supabase = supabaseForRequest(req);

    const [profileQ, goalQ, anthroQ] = await Promise.all([
      supabase.from("profiles").select("*").maybeSingle(),
      supabase.from("nutrition_goals").select("*").is("effective_to", null).maybeSingle(),
      supabase
        .from("anthropometrics")
        .select("*")
        .order("measured_at", { ascending: false })
        .limit(1)
        .maybeSingle(),
    ]);

    const firstError = profileQ.error ?? goalQ.error ?? anthroQ.error;
    if (firstError) {
      req.log.error({ err: firstError }, "me_query_failed");
      return reply.code(500).send({ error: firstError.message });
    }

    if (!profileQ.data) {
      return reply.code(404).send({ error: "profile_not_found" });
    }

    return reply.send({
      profile: profileQ.data,
      nutrition_goal: goalQ.data,
      anthropometric: anthroQ.data,
    });
  });

  app.get("/me/daily-summary", { preHandler: [authRequired] }, async (req, reply) => {
    const parsed = dailySummaryQuerySchema.safeParse(req.query);
    if (!parsed.success) {
      return reply.code(400).send({ error: parsed.error.issues[0]?.message ?? "bad_query" });
    }
    const { day } = parsed.data;
    const userId = req.user!.id;
    const supabase = supabaseForRequest(req);

    // Caso 1: dia explícito → query direta.
    // Caso 2: sem dia → view resolve "hoje" via boundary do user.
    const query = day
      ? supabase.from("daily_summaries").select("*").eq("day", day).maybeSingle()
      : supabase.from("vw_today_summary").select("*").maybeSingle();

    const { data, error } = await query;
    if (error) {
      req.log.error({ err: error }, "daily_summary_query_failed");
      return reply.code(500).send({ error: error.message });
    }

    if (data) {
      return reply.send({ summary: DailySummarySchema.parse(data) });
    }

    // Empty fallback — resolve dia via RPC, busca meta vigente, retorna shape zerado.
    const resolvedDay =
      day ??
      (
        await supabase.rpc("fitbrother_today", { p_user_id: userId })
      ).data;

    if (!resolvedDay) {
      req.log.error({ userId }, "fitbrother_today_returned_null");
      return reply.code(500).send({ error: "could_not_resolve_today" });
    }

    const { data: goal } = await supabase
      .from("nutrition_goals")
      .select("kcal, protein_g, carbs_g, fat_g")
      .lte("effective_from", resolvedDay)
      .or(`effective_to.is.null,effective_to.gte.${resolvedDay}`)
      .order("effective_from", { ascending: false })
      .limit(1)
      .maybeSingle();

    const summary: DailySummary = {
      user_id: userId,
      day: resolvedDay,
      kcal: 0,
      protein_g: 0,
      carbs_g: 0,
      fat_g: 0,
      goal_kcal: goal?.kcal ?? null,
      goal_protein_g: goal?.protein_g ?? null,
      goal_carbs_g: goal?.carbs_g ?? null,
      goal_fat_g: goal?.fat_g ?? null,
      goal_hit: false,
      meals_count: 0,
      updated_at: new Date().toISOString(),
    };

    return reply.send({ summary });
  });
}
```

- [ ] **Step 2: Typecheck server**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/server
```

Expected: sem erros.

- [ ] **Step 3: Verificar dev server reinicia sem erro**

Mate o server anterior se estiver rodando e suba:

```bash
cd /home/pedrobritto/development/fitbrother/apps/server && npm run dev
```

(use `run_in_background: true`)

Aguarde 4 segundos e cheque:

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/health
```

Expected: `200`.

- [ ] **Step 4: Commit**

```bash
git add apps/server/src/routes/me.ts
git commit -m "feat(server): GET /me/daily-summary with empty fallback"
```

---

## Task 5: Script de checks SQL/HTTP

**Files:**
- Create: `scripts/checks/m3-1-daily-summary.sh`
- Create: `scripts/checks/m3-1-daily-summary.sql`

Este script substitui suíte Vitest formal — solo dev MVP, padrão de "smoke checks via psql + curl".

- [ ] **Step 1: Criar SQL com assertions**

Criar `scripts/checks/m3-1-daily-summary.sql`:

```sql
-- M3.1 backend smoke checks. Rodado via:
--   docker exec -i supabase_db_fitbrother psql -U postgres -d postgres < m3-1-daily-summary.sql
--
-- Assume db já populado pelo seed + ao menos 1 user em auth.users.

\set ON_ERROR_STOP on

-- Check 1: vw_today_summary existe e retorna 0 rows quando rodado sem auth.uid().
-- (auth.uid() é NULL sem JWT context — view filtra user_id = NULL → 0 rows.)
SELECT 'check_1_view_empty_without_auth' AS check,
       COUNT(*) = 0 AS pass
FROM public.vw_today_summary;

-- Check 2: fitbrother_today retorna data quando user existe.
-- Pega primeiro user real do banco pra teste.
DO $$
DECLARE
  v_user uuid;
  v_today date;
BEGIN
  SELECT user_id INTO v_user FROM public.profiles LIMIT 1;
  IF v_user IS NULL THEN
    RAISE NOTICE 'check_2_skipped: no user in profiles';
  ELSE
    SELECT public.fitbrother_today(v_user) INTO v_today;
    IF v_today IS NULL THEN
      RAISE EXCEPTION 'check_2_FAIL: fitbrother_today returned NULL for valid user';
    END IF;
    RAISE NOTICE 'check_2_pass: fitbrother_today(%) = %', v_user, v_today;
  END IF;
END $$;

-- Check 3: boundary respeitada — user com day_start_hour=4 e meal às 03:00 local
-- ainda conta como "ontem". Cria user temporário pra validar.
DO $$
DECLARE
  v_user uuid := gen_random_uuid();
  v_yesterday date := (now() AT TIME ZONE 'America/Sao_Paulo')::date - 1;
  v_computed date;
BEGIN
  -- profiles requires auth.users FK; usamos service-role bypass via insert direto
  -- não persistente (transação em DO block faz rollback ao fim).
  INSERT INTO auth.users (id, email) VALUES (v_user, 'check3@local.test') ON CONFLICT DO NOTHING;
  INSERT INTO public.profiles (user_id, timezone, day_start_hour, locale)
    VALUES (v_user, 'America/Sao_Paulo', 4, 'pt-BR')
    ON CONFLICT (user_id) DO UPDATE SET day_start_hour = 4, timezone = 'America/Sao_Paulo';

  -- Simula 03:00 do dia atual local — deve cair em "ontem" porque day_start_hour=4.
  SELECT public.fitbrother_nutritional_day(
    v_user,
    (date_trunc('day', now() AT TIME ZONE 'America/Sao_Paulo') + interval '3 hours') AT TIME ZONE 'America/Sao_Paulo'
  ) INTO v_computed;

  IF v_computed <> v_yesterday THEN
    RAISE EXCEPTION 'check_3_FAIL: expected %, got % (day_start_hour=4, ts=03:00 local)', v_yesterday, v_computed;
  END IF;
  RAISE NOTICE 'check_3_pass: 03:00 local with day_start_hour=4 → %', v_computed;

  -- Cleanup
  DELETE FROM public.profiles WHERE user_id = v_user;
  DELETE FROM auth.users WHERE id = v_user;
END $$;

-- Check 4: GRANT SELECT em vw_today_summary pra anon/authenticated.
SELECT 'check_4_grants_present' AS check,
       COUNT(*) = 2 AS pass
FROM information_schema.role_table_grants
WHERE table_name = 'vw_today_summary'
  AND grantee IN ('anon', 'authenticated')
  AND privilege_type = 'SELECT';

-- Check 5: publication contém daily_summaries e meals.
SELECT 'check_5_publication_tables' AS check,
       COUNT(*) = 2 AS pass
FROM pg_publication_tables
WHERE pubname = 'supabase_realtime'
  AND schemaname = 'public'
  AND tablename IN ('daily_summaries', 'meals');
```

- [ ] **Step 2: Criar script bash que orquestra SQL + HTTP**

Criar `scripts/checks/m3-1-daily-summary.sh`:

```bash
#!/usr/bin/env bash
# M3.1 backend smoke checks — SQL via psql + HTTP via curl com JWT real.
# Pré-condições: supabase local up, server em :3000, ao menos 1 user em auth.users.
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"

echo "── M3.1 checks ──"

# Checks 1-5: SQL
echo "[1-5] SQL checks via psql..."
docker exec -i supabase_db_fitbrother psql -U postgres -d postgres \
  < scripts/checks/m3-1-daily-summary.sql

# Check 6: GET /me/daily-summary sem query → resolve "hoje".
echo "[6] GET /me/daily-summary (server resolves today)..."
JWT="${TEST_USER_JWT:-}"
if [[ -z "$JWT" ]]; then
  echo "  SKIPPED: TEST_USER_JWT not set (export it from app's signed-in session)"
else
  HTTP=$(curl -s -o /tmp/m31-resp1.json -w "%{http_code}" \
    -H "Authorization: Bearer $JWT" \
    http://localhost:3000/me/daily-summary)
  if [[ "$HTTP" != "200" ]]; then
    echo "  FAIL: status $HTTP"; cat /tmp/m31-resp1.json; exit 1
  fi
  if ! grep -q '"day":"[0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\}"' /tmp/m31-resp1.json; then
    echo "  FAIL: response missing day field"; cat /tmp/m31-resp1.json; exit 1
  fi
  echo "  PASS"
fi

# Check 7: GET /me/daily-summary?day=2026-05-20 → row específica.
echo "[7] GET /me/daily-summary?day=2026-05-20 (past day)..."
if [[ -z "$JWT" ]]; then
  echo "  SKIPPED: TEST_USER_JWT not set"
else
  HTTP=$(curl -s -o /tmp/m31-resp2.json -w "%{http_code}" \
    -H "Authorization: Bearer $JWT" \
    "http://localhost:3000/me/daily-summary?day=2026-05-20")
  if [[ "$HTTP" != "200" ]]; then
    echo "  FAIL: status $HTTP"; cat /tmp/m31-resp2.json; exit 1
  fi
  if ! grep -q '"day":"2026-05-20"' /tmp/m31-resp2.json; then
    echo "  FAIL: response day != 2026-05-20"; cat /tmp/m31-resp2.json; exit 1
  fi
  echo "  PASS"
fi

echo "── all checks done ──"
```

- [ ] **Step 3: Marcar executável e rodar**

```bash
chmod +x scripts/checks/m3-1-daily-summary.sh
./scripts/checks/m3-1-daily-summary.sh
```

Expected output: notices `pass` em checks 1-5, checks 6-7 com SKIPPED (JWT não setado) ou PASS.

- [ ] **Step 4: Commit**

```bash
git add scripts/checks/m3-1-daily-summary.sh scripts/checks/m3-1-daily-summary.sql
git commit -m "test(m3.1): SQL + HTTP smoke checks for daily-summary"
```

---

## Task 6: Mobile API — `fetchDailySummary`

**Files:**
- Modify: `apps/mobile/lib/api/me.ts` (criar se não existe)

- [ ] **Step 1: Verificar se arquivo existe**

```bash
ls -la /home/pedrobritto/development/fitbrother/apps/mobile/lib/api/me.ts 2>&1 || echo "NOT_FOUND"
```

- [ ] **Step 2: Criar ou estender o arquivo**

Se `me.ts` NÃO existe, criar `apps/mobile/lib/api/me.ts`:

```ts
import { DailySummaryResponseSchema, type DailySummary } from "@fitbrother/shared";
import { authedFetch } from "@/lib/api";

async function parseOrThrow(res: Response): Promise<unknown> {
  if (res.ok) return res.json();
  const body = (await res.json().catch(() => ({}))) as { error?: string };
  const err = new Error(body.error ?? `request_failed_${res.status}`) as Error & {
    status?: number;
  };
  err.status = res.status;
  throw err;
}

export async function fetchDailySummary(day: string): Promise<DailySummary> {
  const res = await authedFetch(`/me/daily-summary?day=${encodeURIComponent(day)}`);
  const body = await parseOrThrow(res);
  return DailySummaryResponseSchema.parse(body).summary;
}
```

Se `me.ts` JÁ existe, adicionar o import e a função sem duplicar `parseOrThrow` (reuse o local).

- [ ] **Step 3: Typecheck mobile**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/lib/api/me.ts
git commit -m "feat(mobile): fetchDailySummary api client"
```

---

## Task 7: Hook `useDailySummary`

**Files:**
- Create: `apps/mobile/lib/hooks/useDailySummary.ts`

- [ ] **Step 1: Criar o hook**

Criar `apps/mobile/lib/hooks/useDailySummary.ts`:

```ts
import { useQuery } from "@tanstack/react-query";
import { fetchDailySummary } from "@/lib/api/me";

export const dailySummaryKey = (day: string) => ["daily-summary", day] as const;

export function useDailySummary(day: string) {
  return useQuery({
    queryKey: dailySummaryKey(day),
    queryFn: () => fetchDailySummary(day),
    enabled: Boolean(day),
    staleTime: 60_000,
  });
}
```

- [ ] **Step 2: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/lib/hooks/useDailySummary.ts
git commit -m "feat(mobile): useDailySummary react query hook"
```

---

## Task 8: Hook `useDailySummaryRealtime`

**Files:**
- Create: `apps/mobile/lib/hooks/useDailySummaryRealtime.ts`

- [ ] **Step 1: Criar o hook**

Criar `apps/mobile/lib/hooks/useDailySummaryRealtime.ts`:

```ts
import { useEffect } from "react";
import { useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/lib/supabase";
import { dailySummaryKey } from "./useDailySummary";

/**
 * Subscribe ao canal Postgres Changes de daily_summaries filtrando por user_id.
 * Invalida React Query quando chega UPDATE da row do dia ativo.
 *
 * Note: o filtro Postgres só aceita igualdade simples; comparamos `day` no
 * cliente porque UPDATEs em dias passados (edição de meal antigo) também
 * chegam ao canal.
 */
export function useDailySummaryRealtime(userId: string | undefined, day: string) {
  const qc = useQueryClient();

  useEffect(() => {
    if (!userId || !day) return;

    const channel = supabase
      .channel(`ds:${userId}:${day}`)
      .on(
        "postgres_changes",
        {
          event: "UPDATE",
          schema: "public",
          table: "daily_summaries",
          filter: `user_id=eq.${userId}`,
        },
        (payload) => {
          const newDay = (payload.new as { day?: string }).day;
          if (newDay === day) {
            qc.invalidateQueries({ queryKey: dailySummaryKey(day) });
          }
        },
      )
      .subscribe();

    return () => {
      void supabase.removeChannel(channel);
    };
  }, [userId, day, qc]);
}
```

- [ ] **Step 2: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/lib/hooks/useDailySummaryRealtime.ts
git commit -m "feat(mobile): useDailySummaryRealtime channel subscription"
```

---

## Task 9: Hook `useMealsRealtime`

**Files:**
- Create: `apps/mobile/lib/hooks/useMealsRealtime.ts`

- [ ] **Step 1: Criar o hook**

Criar `apps/mobile/lib/hooks/useMealsRealtime.ts`:

```ts
import { useEffect } from "react";
import { useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/lib/supabase";
import { mealsForDayKey } from "./useMealsForDay";
import { dailySummaryKey } from "./useDailySummary";

/**
 * Subscribe a INSERT/UPDATE/DELETE em meals filtrando por user_id.
 * Invalida tanto mealsForDay quanto dailySummary — daily_summaries chega
 * num segundo evento via trigger, e adiantar a invalidação reduz latência
 * percebida.
 *
 * O filtro Postgres só aceita igualdade simples; `day` é deduzido pelo
 * consumed_at de cada payload (boundary não importa aqui — qualquer mudança
 * em meals do user dispara refetch).
 */
export function useMealsRealtime(userId: string | undefined, day: string) {
  const qc = useQueryClient();

  useEffect(() => {
    if (!userId || !day) return;

    const channel = supabase
      .channel(`meals:${userId}:${day}`)
      .on(
        "postgres_changes",
        {
          event: "*",
          schema: "public",
          table: "meals",
          filter: `user_id=eq.${userId}`,
        },
        () => {
          qc.invalidateQueries({ queryKey: mealsForDayKey(day) });
          qc.invalidateQueries({ queryKey: dailySummaryKey(day) });
        },
      )
      .subscribe();

    return () => {
      void supabase.removeChannel(channel);
    };
  }, [userId, day, qc]);
}
```

- [ ] **Step 2: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/lib/hooks/useMealsRealtime.ts
git commit -m "feat(mobile): useMealsRealtime channel subscription"
```

---

## Task 10: Token `calories` em `lib/colors.ts`

**Files:**
- Modify: `apps/mobile/lib/colors.ts`

- [ ] **Step 1: Verificar estrutura atual**

```bash
sed -n '1,60p' /home/pedrobritto/development/fitbrother/apps/mobile/lib/colors.ts
```

Identifica onde `primary` está e onde adicionar alias `calories`.

- [ ] **Step 2: Adicionar alias `calories`**

Adicionar dentro do objeto `colors`, logo após o bloco `primary`:

```ts
  // Alias semântico pro hero ring de calorias. Brand teal é o token de
  // calorias no domínio nutricional (DESIGN_SYSTEM §12.1).
  calories: { 50: "#F0FDFC", 100: "#CCFBF1", 500: "#14B8A6", 600: "#0D9488" },
```

> Valores devem casar com `primary` em `tailwind.config.ts`. Se primary lá usa outro hex, espelhar aqui.

- [ ] **Step 3: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/lib/colors.ts
git commit -m "feat(mobile): add calories color token (alias of primary teal)"
```

---

## Task 11: Componente `ProgressRing`

**Files:**
- Create: `apps/mobile/components/domain/ProgressRing.tsx`

- [ ] **Step 1: Criar o componente**

Criar `apps/mobile/components/domain/ProgressRing.tsx`:

```tsx
import { useEffect } from "react";
import { Text, View } from "react-native";
import Animated, {
  useAnimatedProps,
  useSharedValue,
  withTiming,
} from "react-native-reanimated";
import Svg, { Circle } from "react-native-svg";
import { colors } from "@/lib/colors";
import { Motion } from "@/lib/motion";

const AnimatedCircle = Animated.createAnimatedComponent(Circle);

type RingColor = "protein" | "carbs" | "fat" | "calories";

type Props = {
  value: number;
  max: number | null;
  color: RingColor;
  size?: number;
  strokeWidth?: number;
  centerTop?: string;
  centerBottom?: string;
  accessibilityLabel?: string;
};

function colorFor(c: RingColor): string {
  switch (c) {
    case "protein":
      return colors.protein[500];
    case "carbs":
      return colors.carbs[500];
    case "fat":
      return colors.fat[500];
    case "calories":
      return colors.calories[500];
  }
}

export function ProgressRing({
  value,
  max,
  color,
  size = 80,
  strokeWidth = 8,
  centerTop,
  centerBottom,
  accessibilityLabel,
}: Props) {
  const radius = (size - strokeWidth) / 2;
  const circumference = 2 * Math.PI * radius;

  // Progress 0..1, capped at 1. Se max for null/0, progress=0 (track-only).
  const targetProgress = !max || max <= 0 ? 0 : Math.min(value / max, 1);
  const progress = useSharedValue(0);

  useEffect(() => {
    progress.value = withTiming(targetProgress, {
      duration: Motion.duration.slow,
      easing: Motion.easing.decelerate,
    });
  }, [progress, targetProgress]);

  const animatedProps = useAnimatedProps(() => ({
    strokeDashoffset: circumference * (1 - progress.value),
  }));

  return (
    <View
      accessibilityRole="image"
      accessibilityLabel={accessibilityLabel}
      style={{ width: size, height: size, alignItems: "center", justifyContent: "center" }}
    >
      <Svg
        width={size}
        height={size}
        style={{ position: "absolute", transform: [{ rotate: "-90deg" }] }}
      >
        <Circle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          stroke={colors.neutral[100]}
          strokeWidth={strokeWidth}
          fill="none"
        />
        <AnimatedCircle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          stroke={colorFor(color)}
          strokeWidth={strokeWidth}
          strokeLinecap="round"
          fill="none"
          strokeDasharray={`${circumference} ${circumference}`}
          animatedProps={animatedProps}
        />
      </Svg>
      <View style={{ alignItems: "center" }}>
        {centerTop ? (
          <Text
            className={
              size >= 120
                ? "font-sans-bold text-neutral-900 text-3xl"
                : "font-sans-bold text-neutral-900 text-lg"
            }
            style={{ fontVariant: ["tabular-nums"] }}
          >
            {centerTop}
          </Text>
        ) : null}
        {centerBottom ? (
          <Text
            className="font-sans-medium text-neutral-500 text-xs"
            style={{ fontVariant: ["tabular-nums"] }}
          >
            {centerBottom}
          </Text>
        ) : null}
      </View>
    </View>
  );
}
```

- [ ] **Step 2: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros. Se `react-native-svg` reclamar de tipo no `AnimatedCircle`, ver Risco no spec — fallback é usar `setNativeProps` com `useRef`.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/components/domain/ProgressRing.tsx
git commit -m "feat(mobile): ProgressRing SVG component with Reanimated 4"
```

---

## Task 12: Componente `TodaySummaryHeader`

**Files:**
- Create: `apps/mobile/components/domain/TodaySummaryHeader.tsx`

- [ ] **Step 1: Criar o componente**

Criar `apps/mobile/components/domain/TodaySummaryHeader.tsx`:

```tsx
import { View } from "react-native";
import type { DailySummary } from "@fitbrother/shared";
import { ProgressRing } from "./ProgressRing";

type Props = {
  summary: DailySummary | undefined;
};

function fmtInt(n: number): string {
  return Math.round(n).toLocaleString("pt-BR");
}

function fmtGrams(n: number): string {
  return `${Math.round(n)}g`;
}

export function TodaySummaryHeader({ summary }: Props) {
  // Loading state: rings em 0 + meta nula. Sem animação até dados chegarem.
  const kcal = summary?.kcal ?? 0;
  const protein = summary?.protein_g ?? 0;
  const carbs = summary?.carbs_g ?? 0;
  const fat = summary?.fat_g ?? 0;
  const goalKcal = summary?.goal_kcal ?? null;
  const goalProtein = summary?.goal_protein_g ?? null;
  const goalCarbs = summary?.goal_carbs_g ?? null;
  const goalFat = summary?.goal_fat_g ?? null;

  return (
    <View className="px-6 pt-4 pb-6 items-center gap-6">
      <ProgressRing
        value={kcal}
        max={goalKcal}
        color="calories"
        size={160}
        strokeWidth={14}
        centerTop={fmtInt(kcal)}
        centerBottom={goalKcal ? `/ ${fmtInt(goalKcal)} kcal` : "kcal"}
        accessibilityLabel={
          goalKcal
            ? `${fmtInt(kcal)} de ${fmtInt(goalKcal)} calorias`
            : `${fmtInt(kcal)} calorias`
        }
      />
      <View className="flex-row justify-around w-full px-2">
        <ProgressRing
          value={protein}
          max={goalProtein}
          color="protein"
          centerTop={fmtGrams(protein)}
          centerBottom="proteína"
          accessibilityLabel={
            goalProtein
              ? `${fmtGrams(protein)} de ${fmtGrams(goalProtein)} proteína`
              : `${fmtGrams(protein)} proteína`
          }
        />
        <ProgressRing
          value={carbs}
          max={goalCarbs}
          color="carbs"
          centerTop={fmtGrams(carbs)}
          centerBottom="carboidrato"
          accessibilityLabel={
            goalCarbs
              ? `${fmtGrams(carbs)} de ${fmtGrams(goalCarbs)} carboidrato`
              : `${fmtGrams(carbs)} carboidrato`
          }
        />
        <ProgressRing
          value={fat}
          max={goalFat}
          color="fat"
          centerTop={fmtGrams(fat)}
          centerBottom="gordura"
          accessibilityLabel={
            goalFat ? `${fmtGrams(fat)} de ${fmtGrams(goalFat)} gordura` : `${fmtGrams(fat)} gordura`
          }
        />
      </View>
    </View>
  );
}
```

- [ ] **Step 2: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/components/domain/TodaySummaryHeader.tsx
git commit -m "feat(mobile): TodaySummaryHeader (hero + 3 macro rings)"
```

---

## Task 13: Integração na Home

**Files:**
- Modify: `apps/mobile/app/(app)/index.tsx`

- [ ] **Step 1: Adicionar imports**

No topo de `apps/mobile/app/(app)/index.tsx`, adicionar (manter os imports já existentes):

```tsx
import { useDailySummary } from "@/lib/hooks/useDailySummary";
import { useDailySummaryRealtime } from "@/lib/hooks/useDailySummaryRealtime";
import { useMealsRealtime } from "@/lib/hooks/useMealsRealtime";
import { TodaySummaryHeader } from "@/components/domain/TodaySummaryHeader";
```

- [ ] **Step 2: Adicionar hooks no corpo do componente**

Logo após `const mealsQuery = useMealsForDay(day);` adicionar:

```tsx
  const summaryQuery = useDailySummary(day);
  useDailySummaryRealtime(userId, day);
  useMealsRealtime(userId, day);
```

> `userId` já existe no escopo (linha 47): `const userId = session.status === "signed_in" ? session.session.user.id : undefined;`

- [ ] **Step 3: Adicionar `ListHeaderComponent` no FlatList**

Encontrar o bloco `<Animated.FlatList ... />` (~linha 204) e adicionar a prop `ListHeaderComponent`:

```tsx
        <Animated.FlatList
          data={items}
          keyExtractor={(m) => (m as OptimisticMeal).id}
          renderItem={renderItem as never}
          ListHeaderComponent={<TodaySummaryHeader summary={summaryQuery.data} />}
          contentContainerStyle={{ paddingBottom: 140 }}
          keyboardDismissMode="on-drag"
          keyboardShouldPersistTaps="handled"
          itemLayoutAnimation={LinearTransition.springify().damping(20).stiffness(180)}
          refreshControl={
            <RefreshControl
              refreshing={mealsQuery.isRefetching || summaryQuery.isRefetching}
              onRefresh={() => {
                void mealsQuery.refetch();
                void summaryQuery.refetch();
              }}
              tintColor={colors.primary[400]}
            />
          }
        />
```

- [ ] **Step 4: Mostrar header também no empty state**

Encontrar o bloco do empty state:

```tsx
      ) : items.length === 0 ? (
        <Pressable className="flex-1" onPress={Keyboard.dismiss}>
          <EmptyMealsState />
        </Pressable>
      ) : (
```

Substituir por:

```tsx
      ) : items.length === 0 ? (
        <Pressable className="flex-1" onPress={Keyboard.dismiss}>
          <TodaySummaryHeader summary={summaryQuery.data} />
          <EmptyMealsState />
        </Pressable>
      ) : (
```

> Sem rings em estado vazio é menos informativo do que rings em 0/meta visível.

- [ ] **Step 5: Typecheck**

```bash
cd /home/pedrobritto/development/fitbrother
npm run typecheck -w @fitbrother/mobile
```

Expected: sem erros.

- [ ] **Step 6: Smoke test manual no Expo**

Subir o ambiente:

```bash
cd /home/pedrobritto/development/fitbrother/apps/server && npm run dev
```

(use `run_in_background: true`)

Em paralelo:

```bash
cd /home/pedrobritto/development/fitbrother/apps/mobile && npm run dev
```

(use `run_in_background: true`)

Abrir no Expo Go. Verificar:
- Home renderiza hero kcal + 3 rings.
- Sem refeições: rings em zero + meta visível.
- Com refeições: valores corretos.
- Adicionar uma refeição via composer → ring atualiza (otimista + invalidação).
- Deletar refeição via swipe → ring atualiza em <1s.

- [ ] **Step 7: Smoke multi-device (Realtime)**

Logar mesmo user no Expo Go (device) e no simulator. Em um, adicionar refeição. No outro, observar ring atualizar em <2s sem ação manual.

- [ ] **Step 8: Commit**

```bash
git add apps/mobile/app/\(app\)/index.tsx
git commit -m "feat(mobile): wire dashboard rings + realtime into Home"
```

---

## Task 14: Atualizar PLAN.md com status M3.1

**Files:**
- Modify: `docs/PLAN.md` (linhas 220-244, seção M3)

- [ ] **Step 1: Adicionar nota de status no fim da seção M3**

Encontrar a linha:

```
**Feito quando:** dia com 3 refeições renderiza com macros e rings corretos; deletar meal → ring atualiza em <1s sem refresh manual; 2 dispositivos do mesmo user logados refletem em <2s; edição de `quantity` recalcula totais.
```

Adicionar logo após:

```markdown

**Status M3.1 (rings + realtime):** ✅ implementado em 2026-05-24 via branch `m3-1-dashboard`. ProgressRing SVG animado, 1 hero kcal (160) + 3 rings macro (80) como `ListHeaderComponent` na Home, hooks `useDailySummary` + `useDailySummaryRealtime` + `useMealsRealtime`, view `vw_today_summary` (security_invoker), RPC `fitbrother_today()`, endpoint `GET /me/daily-summary` com empty fallback. Edição inline + history em M3.2.
```

- [ ] **Step 2: Commit**

```bash
git add docs/PLAN.md
git commit -m "docs(plan): mark M3.1 (dashboard rings + realtime) as implemented"
```

---

## Task 15: Abrir PR

- [ ] **Step 1: Push branch**

```bash
git push -u origin m3-1-dashboard
```

- [ ] **Step 2: Criar PR via gh**

```bash
gh pr create --title "M3.1 — Dashboard (Home rings + Realtime)" --body "$(cat <<'EOF'
## Summary
- Home agora mostra 1 hero ring de calorias (160) + 3 rings de macros (80) como `ListHeaderComponent` no FlatList
- Realtime via Supabase channels em `daily_summaries` e `meals` — multi-device sincroniza em <2s
- Backend: view `vw_today_summary` (security_invoker), RPC `fitbrother_today()`, endpoint `GET /me/daily-summary` com empty fallback preservando shape e goal snapshot

## Spec
[docs/superpowers/specs/2026-05-24-m3-1-dashboard-design.md](docs/superpowers/specs/2026-05-24-m3-1-dashboard-design.md)

## Test plan
- [ ] `./scripts/checks/m3-1-daily-summary.sh` passa (5 SQL + 2 HTTP checks)
- [ ] Expo Go: Home renderiza hero + 3 rings com dados do dia
- [ ] Adicionar refeição → ring atualiza em <1s
- [ ] Deletar refeição → ring atualiza em <1s
- [ ] Empty state (0 meals) mostra rings em zero + meta
- [ ] Multi-device: 2 sessões do mesmo user, INSERT em A reflete em B em <2s

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL retornado.

- [ ] **Step 3: Reportar URL ao usuário**

---

## Self-review notes

- Coverage: cada seção do spec mapeia para uma task — migrations (1+2), shared schema (3), backend route (4), tests (5), mobile hooks (6+7+8+9), color token (10), components (11+12), integration (13), docs+PR (14+15).
- Sem placeholders. Cada step tem código ou comando concreto.
- Tipos batem entre tasks: `DailySummary` shared → `fetchDailySummary` → `useDailySummary` → `TodaySummaryHeader` props.
- Hooks usam mesma key convention que o codebase atual (`useMealsForDay`).
- Migration numbering: 0022 + 0023 (último era 0021).
- Risco mencionado no spec sobre `Animated.Circle` em Reanimated 4: a Task 11 usa `Animated.createAnimatedComponent(Circle)` + `useAnimatedProps` — caminho oficial Reanimated 4 + react-native-svg ≥15. Se falhar, fallback documentado é `setNativeProps` via `useRef`.

# M3.3 — Backfill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir registrar refeições em dias passados (janela 7 dias) usando o mesmo composer natural-language do Home, num modal sobre `/history/[day]`.

**Architecture:** Server já aceita `consumed_at` opcional nos POSTs de meal — adiciona-se uma validação de janela. Mobile ganha uma rota modal `app/(app)/history/[day]/new.tsx` que reusa `MealComposer` com prop nova `consumedAt`. Horário ajustável via wheel-picker existente (sem nova dep). Sem migration (trigger de `daily_summaries` já recalcula por dia).

**Tech Stack:** Fastify 5 + Zod (server), Expo Router (modal route), TanStack React Query 5 (invalidations), `@quidone/react-native-wheel-picker` (já instalada).

---

## File Map

**Server:**
- Create: `apps/server/src/lib/dateMath.ts`
- Modify: `apps/server/src/routes/meals.ts` (POST /meals/text e POST /meals/audio)

**Mobile:**
- Create: `apps/mobile/lib/dateMath.ts`
- Modify: `apps/mobile/lib/hooks/useDailySummaries.ts` (re-export local `addDays` from dateMath)
- Modify: `apps/mobile/lib/hooks/useCreateMealText.ts` (invalidação extra quando backfill)
- Modify: `apps/mobile/lib/hooks/useCreateMealAudio.ts` (mesmo)
- Modify: `apps/mobile/lib/profile/types.ts` (export helper `nutritionalToday` already exists — no change here)
- Create: `apps/mobile/components/domain/TimePicker.tsx`
- Create: `apps/mobile/components/domain/BackfillContextBar.tsx`
- Modify: `apps/mobile/components/domain/MealComposer.tsx` (prop `consumedAt`)
- Modify: `apps/mobile/components/domain/ErrorBanner.tsx` (novo variant)
- Modify: `apps/mobile/components/domain/HistoryEmptyDayCard.tsx` (vira Pressable)
- Move + Modify: `apps/mobile/app/(app)/history/[day].tsx` → `apps/mobile/app/(app)/history/[day]/index.tsx` (botão CTA)
- Create: `apps/mobile/app/(app)/history/[day]/new.tsx`

**Scripts:**
- Create: `scripts/checks/m3-3-backfill.sh`

---

## Task 1: Server — `addDaysIso` helper

**Files:**
- Create: `apps/server/src/lib/dateMath.ts`

- [ ] **Step 1: Criar helper puro `addDaysIso`**

Conteúdo de `apps/server/src/lib/dateMath.ts`:

```ts
/**
 * Adiciona N dias a uma data ISO YYYY-MM-DD. Função pura — opera só na
 * string, sem dependência de timezone. Espelho do helper que existe no
 * mobile em `apps/mobile/lib/dateMath.ts`.
 */
export function addDaysIso(iso: string, n: number): string {
  const [y, m, d] = iso.split("-").map(Number);
  if (!y || !m || !d) {
    throw new Error(`addDaysIso: invalid input "${iso}"`);
  }
  // Date.UTC interpreta como UTC, então não há drift por TZ local.
  const ts = Date.UTC(y, m - 1, d);
  const next = new Date(ts + n * 86_400_000);
  const yy = next.getUTCFullYear();
  const mm = String(next.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(next.getUTCDate()).padStart(2, "0");
  return `${yy}-${mm}-${dd}`;
}
```

- [ ] **Step 2: Smoke do helper via `tsx`**

Run:
```bash
cd apps/server && npx tsx -e "import { addDaysIso } from './src/lib/dateMath.js'; console.log(addDaysIso('2026-05-25', -6) === '2026-05-19' ? 'OK' : 'FAIL'); console.log(addDaysIso('2026-03-01', -1) === '2026-02-28' ? 'OK' : 'FAIL'); console.log(addDaysIso('2026-12-31', 1) === '2027-01-01' ? 'OK' : 'FAIL');"
```

Expected: três `OK`.

- [ ] **Step 3: Typecheck**

Run: `cd apps/server && npm run typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add apps/server/src/lib/dateMath.ts
git commit -m "feat(server): addDaysIso helper for backfill window math"
```

---

## Task 2: Server — Validation block em POST /meals/text

**Files:**
- Modify: `apps/server/src/routes/meals.ts` (handler `POST /meals/text`, depois do `parsed.success` check, antes do `extractMeal`)

- [ ] **Step 1: Adicionar import do helper**

No topo de `apps/server/src/routes/meals.ts`, junto dos outros imports do `../lib/`:

```ts
import { addDaysIso } from "../lib/dateMath.js";
```

- [ ] **Step 2: Adicionar bloco de validação em POST /meals/text**

Em `apps/server/src/routes/meals.ts`, no handler `app.post("/meals/text", ...)`, **logo após** a linha:

```ts
const { client_meal_id, text, consumed_at, locale } = parsed.data;
const userId = req.user!.id;
const supabase = supabaseForRequest(req);
```

inserir antes do `let extraction;`:

```ts
if (consumed_at) {
  const { data: backfillDayRaw, error: dayErr } = await supabase.rpc(
    "fitbrother_nutritional_day",
    { p_user_id: userId, p_ts: consumed_at },
  );
  if (dayErr || !backfillDayRaw) {
    req.log.error({ err: dayErr, consumed_at }, "nutritional_day_failed");
    return reply.code(500).send({ error: "nutritional_day_failed" });
  }
  const { data: todayRaw, error: todayErr } = await supabase.rpc(
    "fitbrother_today",
    { p_user_id: userId },
  );
  if (todayErr || !todayRaw) {
    req.log.error({ err: todayErr, userId }, "today_lookup_failed");
    return reply.code(500).send({ error: "today_lookup_failed" });
  }
  const backfillDay = backfillDayRaw as string;
  const today = todayRaw as string;
  const minDay = addDaysIso(today, -6);
  if (backfillDay < minDay || backfillDay > today) {
    return reply.code(400).send({
      error: "backfill_window_exceeded",
      window: { from: minDay, to: today },
    });
  }
}
```

- [ ] **Step 3: Typecheck**

Run: `cd apps/server && npm run typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add apps/server/src/routes/meals.ts
git commit -m "feat(server): validate backfill window on POST /meals/text"
```

---

## Task 3: Server — Validation block em POST /meals/audio

**Files:**
- Modify: `apps/server/src/routes/meals.ts` (handler `POST /meals/audio`, depois do ownership check)

- [ ] **Step 1: Adicionar mesmo bloco em POST /meals/audio**

Em `apps/server/src/routes/meals.ts`, no handler `app.post("/meals/audio", ...)`, **logo após** o bloco de ownership check:

```ts
if (!audio_path.startsWith(`${userId}/`)) {
  return reply.code(403).send({ error: "audio_path_ownership_mismatch" });
}
```

inserir antes do `// 1. Transcribe (with cap + cache).`:

```ts
if (consumed_at) {
  const { data: backfillDayRaw, error: dayErr } = await supabase.rpc(
    "fitbrother_nutritional_day",
    { p_user_id: userId, p_ts: consumed_at },
  );
  if (dayErr || !backfillDayRaw) {
    req.log.error({ err: dayErr, consumed_at }, "nutritional_day_failed");
    return reply.code(500).send({ error: "nutritional_day_failed" });
  }
  const { data: todayRaw, error: todayErr } = await supabase.rpc(
    "fitbrother_today",
    { p_user_id: userId },
  );
  if (todayErr || !todayRaw) {
    req.log.error({ err: todayErr, userId }, "today_lookup_failed");
    return reply.code(500).send({ error: "today_lookup_failed" });
  }
  const backfillDay = backfillDayRaw as string;
  const today = todayRaw as string;
  const minDay = addDaysIso(today, -6);
  if (backfillDay < minDay || backfillDay > today) {
    return reply.code(400).send({
      error: "backfill_window_exceeded",
      window: { from: minDay, to: today },
    });
  }
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/server && npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add apps/server/src/routes/meals.ts
git commit -m "feat(server): validate backfill window on POST /meals/audio"
```

---

## Task 4: Smoke check script

**Files:**
- Create: `scripts/checks/m3-3-backfill.sh`

- [ ] **Step 1: Criar script**

Conteúdo de `scripts/checks/m3-3-backfill.sh`:

```bash
#!/usr/bin/env bash
# M3.3 backfill smoke checks — janela de 7 dias.
# Pré-condições: supabase local up, server em :3000, TEST_USER_JWT setado.
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"

echo "── M3.3 checks ──"

JWT="${TEST_USER_JWT:-}"
if [[ -z "$JWT" ]]; then
  echo "ERROR: TEST_USER_JWT not set"; exit 1
fi

TODAY=$(date -u +%Y-%m-%d)
DAY_MINUS_3="$(date -u -d "$TODAY -3 days" +%Y-%m-%d)"
DAY_MINUS_8="$(date -u -d "$TODAY -8 days" +%Y-%m-%d)"

# Check 1: consumed_at = today-8d → 400 backfill_window_exceeded
echo "[1] POST /meals/text consumed_at=${DAY_MINUS_8}T12:00:00Z → 400..."
HTTP=$(curl -s -o /tmp/m33-resp1.json -w "%{http_code}" \
  -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" \
  -X POST http://localhost:3000/meals/text \
  -d "{\"client_meal_id\":\"$(uuidgen)\",\"text\":\"1 banana\",\"consumed_at\":\"${DAY_MINUS_8}T12:00:00Z\",\"locale\":\"pt-BR\"}")
if [[ "$HTTP" != "400" ]]; then
  echo "  FAIL: expected 400, got $HTTP"; cat /tmp/m33-resp1.json; exit 1
fi
if ! grep -q 'backfill_window_exceeded' /tmp/m33-resp1.json; then
  echo "  FAIL: expected backfill_window_exceeded"; cat /tmp/m33-resp1.json; exit 1
fi
echo "  PASS"

# Check 2: consumed_at = today-3d → 201 + meal criado
echo "[2] POST /meals/text consumed_at=${DAY_MINUS_3}T12:00:00Z → 201..."
HTTP=$(curl -s -o /tmp/m33-resp2.json -w "%{http_code}" \
  -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" \
  -X POST http://localhost:3000/meals/text \
  -d "{\"client_meal_id\":\"$(uuidgen)\",\"text\":\"1 banana\",\"consumed_at\":\"${DAY_MINUS_3}T12:00:00Z\",\"locale\":\"pt-BR\"}")
if [[ "$HTTP" != "201" ]]; then
  echo "  FAIL: expected 201, got $HTTP"; cat /tmp/m33-resp2.json; exit 1
fi
echo "  PASS"

# Check 3: sem consumed_at → 201 (regressão happy path)
echo "[3] POST /meals/text sem consumed_at → 201..."
HTTP=$(curl -s -o /tmp/m33-resp3.json -w "%{http_code}" \
  -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" \
  -X POST http://localhost:3000/meals/text \
  -d "{\"client_meal_id\":\"$(uuidgen)\",\"text\":\"1 banana\",\"locale\":\"pt-BR\"}")
if [[ "$HTTP" != "201" ]]; then
  echo "  FAIL: expected 201, got $HTTP"; cat /tmp/m33-resp3.json; exit 1
fi
echo "  PASS"

# Check 4: GET /meals?day=today-3d → contém meal do check 2
echo "[4] GET /meals?day=${DAY_MINUS_3} → 200 + meal presente..."
HTTP=$(curl -s -o /tmp/m33-resp4.json -w "%{http_code}" \
  -H "Authorization: Bearer $JWT" \
  "http://localhost:3000/meals?day=${DAY_MINUS_3}")
if [[ "$HTTP" != "200" ]]; then
  echo "  FAIL: expected 200, got $HTTP"; cat /tmp/m33-resp4.json; exit 1
fi
if ! grep -q '"banana"' /tmp/m33-resp4.json; then
  echo "  FAIL: meal não encontrado"; cat /tmp/m33-resp4.json; exit 1
fi
echo "  PASS"

# Check 5: GET /me/daily-summaries cobrindo today-3d tem kcal>0
echo "[5] GET /me/daily-summaries?from=${DAY_MINUS_3}&to=${TODAY} → kcal>0..."
HTTP=$(curl -s -o /tmp/m33-resp5.json -w "%{http_code}" \
  -H "Authorization: Bearer $JWT" \
  "http://localhost:3000/me/daily-summaries?from=${DAY_MINUS_3}&to=${TODAY}")
if [[ "$HTTP" != "200" ]]; then
  echo "  FAIL: expected 200, got $HTTP"; cat /tmp/m33-resp5.json; exit 1
fi
# extrai a summary do dia backfillado e checa kcal > 0 via node
KCAL=$(node -e "const d=JSON.parse(require('fs').readFileSync('/tmp/m33-resp5.json','utf8')); const s=d.summaries.find(x=>x.day==='${DAY_MINUS_3}'); console.log(s ? s.kcal : 0);")
if [[ "$KCAL" == "0" ]]; then
  echo "  FAIL: kcal do dia backfillado é 0"; cat /tmp/m33-resp5.json; exit 1
fi
echo "  PASS (kcal=$KCAL)"

echo "── all checks done ──"
```

- [ ] **Step 2: Tornar executável**

Run: `chmod +x scripts/checks/m3-3-backfill.sh`

- [ ] **Step 3: Rodar smoke (server + supabase up + JWT exportado)**

Run:
```bash
# Pré-requisito: TEST_USER_JWT exportado, supabase local up, server em :3000
./scripts/checks/m3-3-backfill.sh
```

Expected: cinco `PASS`, "all checks done".

- [ ] **Step 4: Commit**

```bash
git add scripts/checks/m3-3-backfill.sh
git commit -m "test(server): M3.3 backfill smoke checks"
```

---

## Task 5: Mobile — `dateMath` helper consolidado

**Files:**
- Create: `apps/mobile/lib/dateMath.ts`
- Modify: `apps/mobile/lib/hooks/useDailySummaries.ts` (importa do dateMath em vez de inline)

- [ ] **Step 1: Criar `apps/mobile/lib/dateMath.ts`**

Conteúdo:

```ts
/**
 * Helpers de manipulação de datas em formato YYYY-MM-DD (pure, sem TZ
 * dependency). Para conversão de nutritional-day veja
 * lib/time/nutritional-day.ts.
 */

export function addDaysIso(iso: string, n: number): string {
  const [y, m, d] = iso.split("-").map(Number);
  if (!y || !m || !d) {
    throw new Error(`addDaysIso: invalid input "${iso}"`);
  }
  const ts = Date.UTC(y, m - 1, d);
  const next = new Date(ts + n * 86_400_000);
  const yy = next.getUTCFullYear();
  const mm = String(next.getUTCMonth() + 1).padStart(2, "0");
  const dd = String(next.getUTCDate()).padStart(2, "0");
  return `${yy}-${mm}-${dd}`;
}

/**
 * Retorna um ISO timestamp UTC para o meio-dia (12:00) local do `day`
 * no fuso do profile. Usado como default em backfill: o usuário pode
 * ajustar via TimePicker depois.
 *
 * Ex.: day="2026-05-23", timezone="America/Sao_Paulo" (UTC-3) →
 *      "2026-05-23T15:00:00.000Z" (12:00 BRT = 15:00 UTC)
 */
export function defaultConsumedAtForDay(
  day: string,
  profile: { timezone: string },
): string {
  // Constrói "2026-05-23 12:00:00" no fuso do profile e converte pra UTC.
  // O truque: criamos um Date a partir do que parece "naive local time"
  // mas calculamos o offset comparando duas projeções da mesma instant.
  const [y, m, d] = day.split("-").map(Number);
  // Cria UTC noon como ponto de partida — sempre dentro do mesmo dia
  // civil em qualquer TZ usual (offsets ±14h).
  const utcNoon = Date.UTC(y, m - 1, d, 12, 0, 0);
  // Quanto o relógio local mostraria nesse instante UTC?
  const localStr = new Date(utcNoon).toLocaleString("sv-SE", {
    timeZone: profile.timezone,
  });
  // sv-SE: "YYYY-MM-DD HH:mm:ss"
  const localHour = parseInt(localStr.slice(11, 13), 10);
  // Diferença entre 12 (target local) e o que o local mostrou → ajusta.
  const offsetHours = 12 - localHour;
  return new Date(utcNoon + offsetHours * 3_600_000).toISOString();
}

/**
 * Aplica time-of-day (hour, minute) sobre um ISO existente preservando
 * o resto. Usado pelo TimePicker quando o usuário ajusta o horário do
 * backfill — mantém o `day` (dia civil no fuso do profile) intacto.
 */
export function setTimeOfDayIso(
  iso: string,
  profile: { timezone: string },
  hour: number,
  minute: number,
): string {
  // Recupera o dia civil no fuso do profile.
  const localStr = new Date(iso).toLocaleString("sv-SE", {
    timeZone: profile.timezone,
  });
  const day = localStr.slice(0, 10);
  const [y, m, d] = day.split("-").map(Number);
  const utcCandidate = Date.UTC(y, m - 1, d, hour, minute, 0);
  // Mesmo truque do default: ajusta pelo offset.
  const candidateLocalStr = new Date(utcCandidate).toLocaleString("sv-SE", {
    timeZone: profile.timezone,
  });
  const candidateLocalHour = parseInt(candidateLocalStr.slice(11, 13), 10);
  const candidateLocalMinute = parseInt(candidateLocalStr.slice(14, 16), 10);
  const offsetMinutes =
    (hour - candidateLocalHour) * 60 + (minute - candidateLocalMinute);
  return new Date(utcCandidate + offsetMinutes * 60_000).toISOString();
}

/**
 * Extrai hour e minute do ISO no fuso do profile. Usado pelo TimePicker
 * pra pré-popular os wheels.
 */
export function getTimeOfDayIso(
  iso: string,
  profile: { timezone: string },
): { hour: number; minute: number } {
  const localStr = new Date(iso).toLocaleString("sv-SE", {
    timeZone: profile.timezone,
  });
  return {
    hour: parseInt(localStr.slice(11, 13), 10),
    minute: parseInt(localStr.slice(14, 16), 10),
  };
}
```

- [ ] **Step 2: Refatorar `useDailySummaries.ts` pra usar o novo helper**

Em `apps/mobile/lib/hooks/useDailySummaries.ts`, remover o `function addDays(...)` local e importar de `@/lib/dateMath`:

```ts
import { addDaysIso as addDays } from "@/lib/dateMath";
```

(Alias `addDays` mantém o resto do arquivo intacto.)

- [ ] **Step 3: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/lib/dateMath.ts apps/mobile/lib/hooks/useDailySummaries.ts
git commit -m "feat(mobile): dateMath helpers (addDaysIso, defaultConsumedAtForDay, setTimeOfDayIso)"
```

---

## Task 6: Mobile — `TimePicker` modal component

**Files:**
- Create: `apps/mobile/components/domain/TimePicker.tsx`

- [ ] **Step 1: Criar TimePicker**

Conteúdo de `apps/mobile/components/domain/TimePicker.tsx`:

```tsx
import { useState } from "react";
import { Modal, Pressable, Text, View } from "react-native";
import { WheelPicker } from "@/components/WheelPicker";
import { shadows } from "@/lib/shadows";

type Props = {
  visible: boolean;
  initialHour: number;
  initialMinute: number;
  onCancel: () => void;
  onConfirm: (hour: number, minute: number) => void;
};

export function TimePicker({ visible, initialHour, initialMinute, onCancel, onConfirm }: Props) {
  const [hour, setHour] = useState(initialHour);
  // Arredonda pra step de 5 minutos no init.
  const [minute, setMinute] = useState(Math.round(initialMinute / 5) * 5);

  return (
    <Modal visible={visible} transparent animationType="fade" onRequestClose={onCancel}>
      <Pressable
        onPress={onCancel}
        className="flex-1 items-center justify-center bg-black/40 px-6"
        accessibilityLabel="Fechar seletor de hora"
      >
        <View
          style={shadows.card}
          className="w-full max-w-sm rounded-2xl bg-white p-4"
          onStartShouldSetResponder={() => true}
        >
          <Text className="mb-3 text-center text-base font-sans-semibold text-neutral-800">
            Horário
          </Text>
          <View className="flex-row items-center justify-center">
            <View className="flex-1">
              <WheelPicker
                min={0}
                max={23}
                value={hour}
                onChange={setHour}
              />
            </View>
            <Text className="px-1 text-2xl font-sans-bold text-neutral-700">:</Text>
            <View className="flex-1">
              <WheelPicker
                min={0}
                max={55}
                step={5}
                value={minute}
                onChange={setMinute}
              />
            </View>
          </View>
          <View className="mt-4 flex-row gap-3">
            <Pressable
              onPress={onCancel}
              className="flex-1 min-h-[44px] items-center justify-center rounded-xl bg-neutral-100"
              accessibilityLabel="Cancelar"
              accessibilityRole="button"
            >
              <Text className="text-sm font-sans-medium text-neutral-700">Cancelar</Text>
            </Pressable>
            <Pressable
              onPress={() => onConfirm(hour, minute)}
              className="flex-1 min-h-[44px] items-center justify-center rounded-xl bg-primary-500"
              accessibilityLabel="Confirmar"
              accessibilityRole="button"
            >
              <Text className="text-sm font-sans-semibold text-white">Confirmar</Text>
            </Pressable>
          </View>
        </View>
      </Pressable>
    </Modal>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/components/domain/TimePicker.tsx
git commit -m "feat(mobile): TimePicker modal using existing wheel-picker"
```

---

## Task 7: Mobile — `BackfillContextBar`

**Files:**
- Create: `apps/mobile/components/domain/BackfillContextBar.tsx`

- [ ] **Step 1: Criar componente**

Conteúdo de `apps/mobile/components/domain/BackfillContextBar.tsx`:

```tsx
import { useState } from "react";
import { Pressable, Text, View } from "react-native";
import { ChevronDown } from "lucide-react-native";
import { useProfile } from "@/lib/profile/profile-context";
import { getTimeOfDayIso, setTimeOfDayIso } from "@/lib/dateMath";
import { colors } from "@/lib/colors";
import { TimePicker } from "./TimePicker";

type Props = {
  day: string;
  consumedAt: string;
  onChangeConsumedAt: (iso: string) => void;
};

function formatDayLong(day: string): string {
  const d = new Date(day + "T12:00:00Z");
  return d.toLocaleDateString("pt-BR", {
    weekday: "short",
    day: "2-digit",
    month: "short",
  });
}

function formatHHMM(hour: number, minute: number): string {
  return `${String(hour).padStart(2, "0")}:${String(minute).padStart(2, "0")}`;
}

export function BackfillContextBar({ day, consumedAt, onChangeConsumedAt }: Props) {
  const profile = useProfile();
  const [pickerOpen, setPickerOpen] = useState(false);
  const { hour, minute } = getTimeOfDayIso(consumedAt, profile);

  return (
    <View className="mx-4 mt-2 flex-row items-center justify-between rounded-xl bg-neutral-100 px-3 py-2">
      <Text className="text-sm font-sans-medium text-neutral-700">{formatDayLong(day)}</Text>
      <Pressable
        onPress={() => setPickerOpen(true)}
        accessibilityLabel="Ajustar horário"
        accessibilityRole="button"
        className="min-h-[44px] flex-row items-center gap-1 px-2"
        style={{ fontVariant: ["tabular-nums"] } as never}
      >
        <Text
          className="text-sm font-sans-semibold text-neutral-800"
          style={{ fontVariant: ["tabular-nums"] }}
        >
          {formatHHMM(hour, minute)}
        </Text>
        <ChevronDown size={16} color={colors.neutral[600]} />
      </Pressable>
      <TimePicker
        visible={pickerOpen}
        initialHour={hour}
        initialMinute={minute}
        onCancel={() => setPickerOpen(false)}
        onConfirm={(h, m) => {
          onChangeConsumedAt(setTimeOfDayIso(consumedAt, profile, h, m));
          setPickerOpen(false);
        }}
      />
    </View>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/components/domain/BackfillContextBar.tsx
git commit -m "feat(mobile): BackfillContextBar with tap-to-edit time"
```

---

## Task 8: Mobile — `MealComposer` aceita prop `consumedAt`

**Files:**
- Modify: `apps/mobile/components/domain/MealComposer.tsx`

**Contexto:** `MealComposer` hoje recebe `onSend(text)` e `onAudioReady({fileUri, durationMs, ext})`. As mutations são chamadas pelo HomeScreen. Pra backfill, o modal `history/[day]/new.tsx` precisa passar o `consumedAt` em ambos os callbacks. A forma mais clean é o composer expor `onSend` e `onAudioReady` recebendo um arg adicional opcional — mas isso quebra Home. **Solução escolhida:** props inalteradas; em vez disso, o caller (Home ou modal) passa o `consumed_at` na própria mutation. Composer apenas precisa **disable internal text reset** se quiser controle externo de submit — mas como Home já reseta dentro de `onSend`, nada muda no composer em si.

→ **Conclusão: nenhuma mudança no MealComposer**. Toda a propagação acontece no novo `history/[day]/new.tsx`, que mantém seu próprio estado e chama as mutations diretamente.

- [ ] **Step 1: Marcar task como skip**

Sem código a editar. Documentar a decisão neste passo.

- [ ] **Step 2: Commit (vazio se não houver mudança em outros lugares)**

Pular esta task — não há mudança a comitar.

---

## Task 9: Mobile — Hooks invalidam history quando backfill

**Files:**
- Modify: `apps/mobile/lib/hooks/useCreateMealText.ts`
- Modify: `apps/mobile/lib/hooks/useCreateMealAudio.ts`

- [ ] **Step 1: Importar `dailySummariesHistoryKey` em `useCreateMealText.ts`**

Em `apps/mobile/lib/hooks/useCreateMealText.ts`, no topo, ao lado dos outros imports:

```ts
import { dailySummariesHistoryKey } from "./useDailySummaries";
```

- [ ] **Step 2: Invalidar history em `onSuccess` quando `args.consumed_at` está presente**

Substituir o handler `onSuccess` por:

```ts
onSuccess: (result, args) => {
  qc.setQueryData<MealResponse[]>(mealsForDayKey(args.day), (old) => {
    if (!old) return [result.meal];
    return old.map((m) => (m.id === args.client_meal_id ? result.meal : m));
  });
  qc.setQueryData(mealDetailKey(result.meal.id), result.meal);
  // Backfill: reflete a mudança no infinite scroll de /history.
  if (args.consumed_at) {
    qc.invalidateQueries({ queryKey: dailySummariesHistoryKey });
  }
},
```

- [ ] **Step 3: Mesma mudança em `useCreateMealAudio.ts`**

Mesmo import e mesma adição no `onSuccess`:

```ts
import { dailySummariesHistoryKey } from "./useDailySummaries";
```

```ts
onSuccess: (result, args) => {
  qc.setQueryData<MealResponse[]>(mealsForDayKey(args.day), (old) => {
    if (!old) return [result.meal];
    return old.map((m) => (m.id === args.client_meal_id ? result.meal : m));
  });
  qc.setQueryData(mealDetailKey(result.meal.id), result.meal);
  if (args.consumed_at) {
    qc.invalidateQueries({ queryKey: dailySummariesHistoryKey });
  }
},
```

- [ ] **Step 4: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add apps/mobile/lib/hooks/useCreateMealText.ts apps/mobile/lib/hooks/useCreateMealAudio.ts
git commit -m "feat(mobile): invalidate history infinite-scroll on backfill create"
```

---

## Task 10: Mobile — Mover `history/[day].tsx` para pasta

**Files:**
- Move: `apps/mobile/app/(app)/history/[day].tsx` → `apps/mobile/app/(app)/history/[day]/index.tsx`

- [ ] **Step 1: Mover arquivo via git**

Run:
```bash
mkdir -p apps/mobile/app/\(app\)/history/\[day\]
git mv apps/mobile/app/\(app\)/history/\[day\].tsx apps/mobile/app/\(app\)/history/\[day\]/index.tsx
```

- [ ] **Step 2: Verificar Expo Router resolve**

Run:
```bash
cd apps/mobile && npm run typecheck
```
Expected: no errors. (Expo Router trata `[day]/index.tsx` igual a `[day].tsx` — rota `/history/:day`.)

- [ ] **Step 3: Commit**

```bash
git add -A apps/mobile/app/\(app\)/history/
git commit -m "refactor(mobile): move history/[day].tsx to [day]/index.tsx (coexist with new.tsx)"
```

---

## Task 11: Mobile — Botão CTA "+ Registrar refeição" em `history/[day]/index.tsx`

**Files:**
- Modify: `apps/mobile/app/(app)/history/[day]/index.tsx`

- [ ] **Step 1: Adicionar imports e helper de janela**

No topo de `apps/mobile/app/(app)/history/[day]/index.tsx`, adicionar:

```ts
import { Plus } from "lucide-react-native";
import { addDaysIso } from "@/lib/dateMath";
import { nutritionalToday } from "@/lib/time/nutritional-day";
import { useProfile } from "@/lib/profile/profile-context";
```

(Manter os imports já existentes — `useLocalSearchParams`, `useRouter`, etc.)

- [ ] **Step 2: Computar `canBackfill` no componente**

Dentro de `HistoryDayScreen()`, depois de `const router = useRouter();`, adicionar:

```ts
const profile = useProfile();
const today = nutritionalToday(profile);
const canBackfill = !!day && day !== today && day >= addDaysIso(today, -6);
```

- [ ] **Step 3: Adicionar botão CTA no header**

No JSX do header (a `<View className="flex-row items-center px-4 py-2">`), depois do título `<Text className="ml-2 flex-1 ...">{formatDayHeader(day)}</Text>`, antes do fechamento da View, adicionar:

```tsx
{canBackfill && (
  <Pressable
    onPress={() =>
      router.push({
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        pathname: "/(app)/history/[day]/new" as any,
        params: { day },
      })
    }
    accessibilityLabel="Registrar refeição neste dia"
    accessibilityRole="button"
    className="min-h-[44px] min-w-[44px] flex-row items-center gap-1 rounded-full bg-primary-500 px-3"
  >
    <Plus size={16} color="#ffffff" />
    <Text className="text-xs font-sans-semibold text-white">Registrar</Text>
  </Pressable>
)}
```

- [ ] **Step 4: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add apps/mobile/app/\(app\)/history/\[day\]/index.tsx
git commit -m "feat(mobile): + Registrar refeição CTA on history/[day] within 7d window"
```

---

## Task 12: Mobile — Modal route `history/[day]/new.tsx`

**Files:**
- Create: `apps/mobile/app/(app)/history/[day]/new.tsx`

- [ ] **Step 1: Criar a rota**

Conteúdo de `apps/mobile/app/(app)/history/[day]/new.tsx`:

```tsx
import { useCallback, useState } from "react";
import { Alert, Keyboard, Pressable, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { Stack, useLocalSearchParams, useRouter } from "expo-router";
import { X } from "lucide-react-native";
import * as Localization from "expo-localization";
import { useProfile } from "@/lib/profile/profile-context";
import { defaultConsumedAtForDay } from "@/lib/dateMath";
import {
  newClientMealId,
  useCreateMealText,
} from "@/lib/hooks/useCreateMealText";
import { useCreateMealAudio } from "@/lib/hooks/useCreateMealAudio";
import { uploadMealAudio } from "@/lib/storage";
import { useAuthSession } from "@/lib/hooks/useAuthSession";
import { QuotaExceededError, getErrorStatus } from "@/lib/api/meals";
import { colors } from "@/lib/colors";
import { MealComposer } from "@/components/domain/MealComposer";
import { BackfillContextBar } from "@/components/domain/BackfillContextBar";
import { ErrorBanner, type ErrorBannerVariant } from "@/components/domain/ErrorBanner";

function detectLocale(): string {
  return Localization.getLocales()[0]?.languageTag ?? "pt-BR";
}

function formatDayHeader(day: string): string {
  const d = new Date(day + "T12:00:00Z");
  return d.toLocaleDateString("pt-BR", {
    weekday: "long",
    day: "2-digit",
    month: "short",
  });
}

export default function BackfillScreen() {
  const { day } = useLocalSearchParams<{ day: string }>();
  const router = useRouter();
  const profile = useProfile();
  const session = useAuthSession();
  const userId = session.status === "signed_in" ? session.session.user.id : undefined;
  const createMeal = useCreateMealText();
  const createMealAudio = useCreateMealAudio();
  const [banner, setBanner] = useState<ErrorBannerVariant | null>(null);
  const [consumedAt, setConsumedAt] = useState(() =>
    day ? defaultConsumedAtForDay(day, profile) : new Date().toISOString(),
  );

  const handleSend = useCallback(
    (text: string) => {
      if (!day) return;
      setBanner(null);
      Keyboard.dismiss();
      createMeal.mutate(
        {
          client_meal_id: newClientMealId(),
          text,
          consumed_at: consumedAt,
          locale: detectLocale(),
          day,
        },
        {
          onSuccess: () => router.back(),
          onError: (err) => {
            if (err instanceof QuotaExceededError) {
              setBanner("quota_exceeded");
            } else if (err.message === "backfill_window_exceeded") {
              setBanner("backfill_window_exceeded");
            } else if (err.message === "request_timeout") {
              setBanner("offline");
            } else if ((getErrorStatus(err) ?? 0) >= 500) {
              setBanner("server_error");
            } else {
              setBanner("network");
            }
          },
        },
      );
    },
    [createMeal, consumedAt, day, router],
  );

  const handleAudioReady = useCallback(
    async (params: { fileUri: string; durationMs: number; ext: "m4a" | "opus" }) => {
      if (!userId || !day) return;
      setBanner(null);
      const client_meal_id = newClientMealId();
      const duration_s = Math.max(1, Math.round(params.durationMs / 1000));
      try {
        const { path } = await uploadMealAudio({
          userId,
          mealId: client_meal_id,
          fileUri: params.fileUri,
          ext: params.ext,
        });
        createMealAudio.mutate(
          {
            client_meal_id,
            audio_path: path,
            duration_s,
            consumed_at: consumedAt,
            locale: detectLocale(),
            day,
          },
          {
            onSuccess: () => router.back(),
            onError: (err) => {
              if (err instanceof QuotaExceededError) {
                setBanner("quota_exceeded");
              } else if (err.message === "backfill_window_exceeded") {
                setBanner("backfill_window_exceeded");
              } else if (err.message === "empty_transcription") {
                setBanner("network");
              } else if (err.message === "request_timeout") {
                setBanner("offline");
              } else if ((getErrorStatus(err) ?? 0) >= 500) {
                setBanner("server_error");
              } else {
                setBanner("network");
              }
            },
          },
        );
      } catch (err) {
        // eslint-disable-next-line no-console
        console.warn("[backfill] upload failed:", err);
        setBanner("network");
      }
    },
    [createMealAudio, consumedAt, day, router, userId],
  );

  if (!day) {
    return (
      <SafeAreaView className="flex-1 items-center justify-center bg-neutral-50">
        <Text className="text-base font-sans text-neutral-600">Dia inválido.</Text>
      </SafeAreaView>
    );
  }

  const processing = createMeal.isPending || createMealAudio.isPending;

  return (
    <SafeAreaView className="flex-1 bg-neutral-50" edges={["top", "left", "right"]}>
      <Stack.Screen options={{ presentation: "modal", headerShown: false }} />
      <View className="flex-row items-center px-4 py-2">
        <Pressable
          onPress={() => router.back()}
          accessibilityLabel="Fechar"
          accessibilityRole="button"
          className="min-h-[44px] min-w-[44px] items-center justify-center"
        >
          <X size={24} color={colors.neutral[800]} />
        </Pressable>
        <Text className="ml-2 flex-1 text-lg font-sans-bold text-neutral-800">
          {formatDayHeader(day)}
        </Text>
      </View>
      <BackfillContextBar
        day={day}
        consumedAt={consumedAt}
        onChangeConsumedAt={setConsumedAt}
      />
      {banner && <ErrorBanner variant={banner} onDismiss={() => setBanner(null)} />}
      <View className="flex-1 justify-end">
        <MealComposer
          onSend={handleSend}
          onAudioReady={handleAudioReady}
          processing={processing}
        />
      </View>
    </SafeAreaView>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors. (Se `"backfill_window_exceeded"` ainda não existe em `ErrorBannerVariant`, ele será adicionado na Task 13 — typecheck pode falhar aqui temporariamente. Se falhar, pule pro Step 3 da Task 13 antes e volte.)

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/app/\(app\)/history/\[day\]/new.tsx
git commit -m "feat(mobile): modal route history/[day]/new with composer + context bar"
```

---

## Task 13: Mobile — ErrorBanner ganha variant `backfill_window_exceeded`

**Files:**
- Modify: `apps/mobile/components/domain/ErrorBanner.tsx`

- [ ] **Step 1: Adicionar variant ao type**

Em `apps/mobile/components/domain/ErrorBanner.tsx`:

```ts
export type ErrorBannerVariant =
  | "quota_exceeded"
  | "offline"
  | "server_error"
  | "network"
  | "backfill_window_exceeded";
```

- [ ] **Step 2: Adicionar copy ao mapa**

No `const COPY: Record<ErrorBannerVariant, ...>`, adicionar entrada:

```ts
backfill_window_exceeded: {
  title: "Janela expirada",
  body: "Só é possível registrar refeições dos últimos 7 dias. Atualize o histórico.",
},
```

- [ ] **Step 3: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add apps/mobile/components/domain/ErrorBanner.tsx
git commit -m "feat(mobile): ErrorBanner variant backfill_window_exceeded"
```

---

## Task 14: Mobile — `HistoryEmptyDayCard` vira Pressable

**Files:**
- Modify: `apps/mobile/components/domain/HistoryEmptyDayCard.tsx`

- [ ] **Step 1: Substituir o componente**

Substituir o conteúdo de `apps/mobile/components/domain/HistoryEmptyDayCard.tsx` por:

```tsx
import { Pressable, Text, View } from "react-native";
import { useRouter } from "expo-router";
import { MoonStar } from "lucide-react-native";
import { colors } from "@/lib/colors";
import { shadows } from "@/lib/shadows";

type Props = {
  day: string;
};

function formatDayHeader(day: string): string {
  const d = new Date(day + "T12:00:00Z");
  return d.toLocaleDateString("pt-BR", {
    weekday: "short",
    day: "2-digit",
    month: "short",
    year: "numeric",
  });
}

export function HistoryEmptyDayCard({ day }: Props) {
  const router = useRouter();
  return (
    <View className="mx-4 mt-3">
      <Text className="ml-1 mb-2 text-xs font-sans-semibold uppercase text-neutral-400">
        {formatDayHeader(day)}
      </Text>
      <Pressable
        onPress={() =>
          router.push({
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            pathname: "/(app)/history/[day]" as any,
            params: { day },
          })
        }
        accessibilityLabel={`Ver dia ${formatDayHeader(day)} (sem refeições registradas)`}
        accessibilityRole="button"
        style={[shadows.card, { opacity: 0.65 }]}
        className="rounded-2xl bg-white p-4 items-center"
      >
        <MoonStar size={20} color={colors.neutral[400]} />
        <Text className="mt-2 text-sm font-sans-medium text-neutral-600">
          Nenhuma refeição registrada
        </Text>
        <Text className="mt-1 text-xs font-sans text-neutral-500 text-center">
          Que tal não deixar passar mais um dia?
        </Text>
      </Pressable>
    </View>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/mobile && npm run typecheck`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add apps/mobile/components/domain/HistoryEmptyDayCard.tsx
git commit -m "feat(mobile): HistoryEmptyDayCard tappable → drills into /history/[day]"
```

---

## Task 15: QA manual end-to-end

- [ ] **Step 1: Subir ambiente (run-app skill)**

Run: (via Skill tool, ou manualmente)
- supabase start
- apps/server: `npm run dev`
- apps/mobile: `npm run dev`

- [ ] **Step 2: Smoke server**

Run:
```bash
export TEST_USER_JWT="<token>"
./scripts/checks/m3-3-backfill.sh
```
Expected: cinco `PASS`.

- [ ] **Step 3: QA mobile — entrar em dia passado vazio**

1. Abrir `/history` no app.
2. Scrollar até ver um empty card (dia sem refeição) dentro dos últimos 7 dias.
3. Tapar no empty card → navega para `/history/[day]`.
4. Header deve mostrar botão "+ Registrar" (azul, redondo).

- [ ] **Step 4: QA mobile — backfill texto**

1. Tapar "+ Registrar" → modal abre com BackfillContextBar mostrando data + "12:00 ▼".
2. Tapar no horário → TimePicker abre.
3. Ajustar para "08:30" → Confirmar.
4. Digitar "café com pão" → enviar.
5. Modal fecha; volta pra `/history/[day]` com o meal aparecendo na lista.
6. Voltar pra `/history` → o card daquele dia agora mostra macros (não é mais empty).

- [ ] **Step 5: QA mobile — backfill áudio**

1. Repetir steps 1-2 do step anterior, mas agora hold-to-record no composer.
2. Após release, esperar processing → modal fecha → meal aparece.

- [ ] **Step 6: QA mobile — fora da janela**

1. Scrollar mais pra trás no `/history` (mais de 7 dias atrás).
2. Tapar em empty card → drilla mesmo assim em `/history/[day]`.
3. Header NÃO deve mostrar botão "+ Registrar" (canBackfill=false).

- [ ] **Step 7: QA mobile — server reject (caso de borda)**

(Manual com Postman/curl simulando data forjada, ou pular se step 2 já cobre.)

- [ ] **Step 8: Sem novo commit (QA passou)**

Se tudo passou, nada a comitar — pular pra finishing.

Se algum bug encontrado, criar commit fix(...): apropriado.

---

## Self-Review (já feito)

- **Spec coverage:**
  - "Modal route `history/[day]/new`" → Task 10, 12
  - "HistoryEmptyDayCard vira tapável" → Task 14
  - "Botão + Registrar refeição com canBackfill" → Task 11
  - "MealComposer ganha prop consumedAt" → Task 8 (skip — caller passa direto na mutation; arquitetura final mais limpa)
  - "Server: validate window" → Tasks 1, 2, 3
  - "Smoke checks" → Task 4
  - "Default 12:00 + time picker opcional" → Tasks 5, 6, 7
  - "Invalidação extra de dailySummariesHistoryKey" → Task 9
  - "Copy backfill_window_exceeded" → Task 13
  - "Sem migration" → confirmado, plano não toca em `supabase/migrations/`
  - "Out of scope: mover meal existente, WhatsApp, bulk import" → não há task pra esses
- **Placeholders:** nenhum "TBD", "implement later", etc.
- **Type consistency:** `addDaysIso` é o mesmo nome em server e mobile; `dailySummariesHistoryKey` é o export real de `useDailySummaries.ts`; `BackfillContextBar` props (`day`, `consumedAt`, `onChangeConsumedAt`) batem entre Task 7 e Task 12.

---

## Final: finishing the branch

- [ ] **Step 1: Verificar tests**

(Sem unit tests no projeto — smoke já rodou em Task 4 e Task 15. Typecheck rodou em cada task. ESLint roda no pre-commit.)

- [ ] **Step 2: Invocar `superpowers:finishing-a-development-branch`**

Anunciar: "I'm using the finishing-a-development-branch skill to complete this work."

Sub-skill apresenta as 4 opções (merge local / PR / keep / discard). Selecionar conforme preferência do usuário.

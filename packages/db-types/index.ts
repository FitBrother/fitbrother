/**
 * Regenerate with: `npm run db:types` (runs `supabase gen types typescript --local`).
 * This placeholder will be overwritten once migrations exist in M1.
 */
export type Database = Record<string, never>;
